{
  "name": "zmknote-tests",
  "version": "1.0.0",
  "description": "Vitest regression tests for Zotero→Obsidian JS sender scripts",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
